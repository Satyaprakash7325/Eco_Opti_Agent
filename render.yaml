services:
  - type: web
    name: eco-opti-agent
    env: python
    buildCommand: "pip install -r backened/requirement.txt"
    startCommand: "cd backened && gunicorn main:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: GOOGLE_API_KEY
        sync: false
